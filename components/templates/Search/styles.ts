import styled from 'styled-components';

export const SearchContainer = styled.div`
  display: flex;
  gap: ${props => props.theme.spacing.margin.lg};
  margin: 0 auto;
  padding: ${props => props.theme.spacing.padding.lg};
`;
